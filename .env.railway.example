# =====================================================
# Railway Environment Variables Configuration
# =====================================================
# Copy these variables to your Railway dashboard
# under Variables tab in your service settings.
# =====================================================

# Application Settings
APP_NAME="Sistem Perizinan Reklame"
APP_ENV=production
APP_DEBUG=false
APP_KEY=
# Note: Run `php artisan key:generate --show` locally and paste the result here
# Example: APP_KEY=base64:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx=

# APP_URL will be auto-generated by Railway
# Example: APP_URL=https://your-app.up.railway.app

# Locale Settings (Indonesia)
APP_LOCALE=id
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=id_ID
APP_TIMEZONE=Asia/Jakarta

# Database Configuration
# For Railway PostgreSQL addon:
# These variables are automatically provided when you add PostgreSQL service
# DB_CONNECTION=pgsql
# DATABASE_URL=${{Postgres.DATABASE_URL}}
# Note: Laravel can parse DATABASE_URL automatically

# For testing with SQLite (no external database needed):
DB_CONNECTION=sqlite
# DB_DATABASE will use default database/database.sqlite

# Session & Cache (using database for Railway compatibility)
SESSION_DRIVER=database
SESSION_LIFETIME=120
CACHE_STORE=database
QUEUE_CONNECTION=database

# Filesystem (local storage for file uploads)
FILESYSTEM_DISK=local

# Logging
LOG_CHANNEL=stack
LOG_STACK=single
LOG_LEVEL=error

# Mail (use log for testing, configure SMTP for production)
MAIL_MAILER=log
MAIL_FROM_ADDRESS="noreply@perizinan.com"
MAIL_FROM_NAME="${APP_NAME}"

# Security
BCRYPT_ROUNDS=12

# =====================================================
# Quick Setup Guide for Railway:
# =====================================================
# 1. Fork/connect this repo to Railway
# 2. Add a new service from your repo
# 3. Go to Variables tab and add these environment variables
# 4. Generate APP_KEY locally: php artisan key:generate --show
# 5. (Optional) Add PostgreSQL service for production database
# 6. Deploy!
# =====================================================
